---
import DashboardLayout from '../../layouts/DashboardLayout.astro';
import { ArrowLeft, FilePlus2, Sparkles } from 'lucide-astro';
---

<DashboardLayout title="Sertifika Ekle">
  <div class="max-w-5xl mx-auto pb-24 space-y-6">
    <div class="flex items-center gap-3">
      <a id="cert-back-link" href="/company" class="flex items-center gap-2 text-xs font-bold text-muted hover:text-primary transition-colors group">
        <ArrowLeft size={14} class="group-hover:-translate-x-1 transition-transform" />
        Firma Profili
      </a>
      <span class="text-border-main">/</span>
      <span class="text-xs font-bold text-muted" id="cert-header-breadcrumb">Sertifika Ekle</span>
    </div>

    <section class="bg-surface border border-border-main shadow-xl rounded-[2rem] overflow-hidden">
      <div id="cert-page-skeleton" class="p-6 sm:p-8 animate-pulse space-y-4">
        <div class="h-3 w-28 rounded bg-surface-hover"></div>
        <div class="h-8 w-72 rounded bg-surface-hover"></div>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div class="h-28 rounded-3xl bg-surface-hover"></div>
          <div class="h-28 rounded-3xl bg-surface-hover"></div>
        </div>
      </div>

      <div id="cert-page-error" class="hidden p-8">
        <p class="text-sm font-bold text-rose-400">Firma context'i yuklenemedi.</p>
      </div>

      <div id="cert-page-shell" class="hidden">
        <div class="p-6 sm:p-8 border-b border-border-main space-y-4">
          <div class="flex flex-wrap items-center gap-2">
            <span class="text-[10px] font-black uppercase tracking-[0.24em] px-2.5 py-1 rounded-full border border-indigo-500/20 bg-indigo-500/10 text-indigo-400" id="cert-company-id">M0</span>
            <span class="text-[10px] font-black uppercase tracking-[0.24em] px-2.5 py-1 rounded-full border border-border-main bg-surface-hover text-muted" id="cert-company-nick">Firma</span>
            <span class="text-[10px] font-black uppercase tracking-[0.24em] px-2.5 py-1 rounded-full border border-emerald-500/20 bg-emerald-500/10 text-emerald-400" id="cert-company-dan">Danisman</span>
          </div>

          <div class="space-y-2">
            <h1 class="text-2xl sm:text-3xl font-extrabold tracking-tight text-main" id="cert-company-unvan">Firma yukleniyor...</h1>
            <p class="text-sm leading-relaxed text-muted max-w-3xl">Legacy sertifika ekleme carousel kartinin mobil uyumlu modern karsiligidir. Standart secimi, kapsam kopyalama ve sertifika no onerisi bu ekranda yonetilir.</p>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
            <div class="rounded-2xl border border-border-main bg-surface-hover p-4">
              <p class="text-[10px] font-bold uppercase tracking-widest text-muted mb-1">Adres</p>
              <p class="text-sm font-semibold text-main leading-relaxed" id="cert-company-address">—</p>
            </div>
            <div class="rounded-2xl border border-border-main bg-surface-hover p-4">
              <p class="text-[10px] font-bold uppercase tracking-widest text-muted mb-1">Mevcut Kapsam</p>
              <p class="text-sm font-semibold text-main leading-relaxed line-clamp-4" id="cert-company-kapsam">—</p>
            </div>
            <div class="rounded-2xl border border-border-main bg-surface-hover p-4">
              <p class="text-[10px] font-bold uppercase tracking-widest text-muted mb-1">Hazir Veri</p>
              <p class="text-sm font-semibold text-main leading-relaxed" id="cert-company-stats">—</p>
            </div>
          </div>
        </div>

        <form id="certificate-form" class="p-6 sm:p-8 space-y-6">
          <section class="grid grid-cols-1 xl:grid-cols-2 gap-6">
            <div class="rounded-3xl border border-border-main bg-surface p-5 sm:p-6 space-y-4">
              <div class="flex items-center justify-between gap-3">
                <h2 class="text-sm font-black uppercase tracking-widest text-main">Temel Alanlar</h2>
                <span class="text-[10px] font-bold px-2.5 py-1 rounded-full border border-border-main bg-surface-hover text-muted">Adim 1</span>
              </div>
              <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Denetim Tipi</label>
                  <select id="cert-denetim" name="denetim" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main">
                    <option value="Belgelendirme">Belgelendirme</option>
                    <option value="Gozetim">Gozetim</option>
                    <option value="Transfer">Transfer</option>
                    <option value="Belge Yenileme">Belge Yenileme</option>
                    <option value="Kapsam Genisletme">Kapsam Genisletme</option>
                  </select>
                </div>
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Standart</label>
                  <select id="cert-standart" name="standart" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main">
                    <option value="">Yukleniyor...</option>
                  </select>
                </div>
              </div>
              <div id="other-standard-wrap" class="space-y-2 hidden">
                <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Diger Standart</label>
                <input id="cert-other" name="other" type="text" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main" placeholder="Diger standart" />
              </div>
              <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">ISO Tam Adi</label>
                  <input id="cert-iso-full" type="text" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main" readonly />
                </div>
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Firma Kisa Adi</label>
                  <input id="cert-nick" type="text" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main" readonly />
                </div>
              </div>
            </div>

            <div class="rounded-3xl border border-border-main bg-surface p-5 sm:p-6 space-y-4">
              <div class="flex items-center justify-between gap-3">
                <h2 class="text-sm font-black uppercase tracking-widest text-main">Tarihler</h2>
                <button id="cert-auto-goz" type="button" class="inline-flex items-center gap-2 rounded-2xl border border-emerald-500/20 bg-emerald-500/10 px-3 py-2 text-[11px] font-black uppercase tracking-widest text-emerald-300 hover:bg-emerald-500/15 transition-colors">
                  <Sparkles size={12} />
                  Gozetim Oner
                </button>
              </div>
              <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Gecerli Sertifika Tarihi</label>
                  <input id="cert-gst" name="gstRaw" type="date" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main" />
                </div>
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Gozetim Tarihi</label>
                  <input id="cert-goz" name="gozRaw" type="date" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main" />
                </div>
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Tescil Tarihi</label>
                  <input id="cert-stt" name="sttRaw" type="date" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main" />
                </div>
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Gecerlilik Tarihi</label>
                  <input id="cert-sgt" name="sgtRaw" type="date" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main" />
                </div>
              </div>
            </div>
          </section>

          <section class="grid grid-cols-1 xl:grid-cols-[1.2fr_0.8fr] gap-6">
            <div class="rounded-3xl border border-border-main bg-surface p-5 sm:p-6 space-y-4">
              <div class="flex items-center justify-between gap-3">
                <h2 class="text-sm font-black uppercase tracking-widest text-main">Sertifika Bilgileri</h2>
                <button id="cert-fill-from-company" type="button" class="inline-flex items-center gap-2 rounded-2xl border border-border-main bg-surface-hover px-3 py-2 text-[11px] font-black uppercase tracking-widest text-main hover:bg-surface transition-colors">Firma Kapsamini Getir</button>
              </div>
              <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Sertifika No</label>
                  <input id="cert-sno" name="sno" type="text" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main" placeholder="Sertifika numarasi" />
                </div>
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Oneri sNo</label>
                  <div class="flex gap-2">
                    <input id="cert-sno-suggested" type="text" class="flex-1 rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main" readonly />
                    <button id="cert-apply-suggested" type="button" class="rounded-2xl border border-emerald-500/20 bg-emerald-500/10 px-4 py-3 text-[11px] font-black uppercase tracking-widest text-emerald-300 hover:bg-emerald-500/15 transition-colors">Uygula</button>
                  </div>
                </div>
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Akreditasyon</label>
                  <input id="cert-akreditasyon" name="akreditasyon" type="text" value="IAKS" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main" />
                </div>
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Akredite Kurulus</label>
                  <input id="cert-akredite" name="akredite" type="text" value="Medicert" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main" />
                </div>
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Kod / NACE</label>
                  <input id="cert-kod" name="kod" type="text" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main" />
                </div>
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Danisman</label>
                  <input id="cert-dan" name="dan" list="consultant-list" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main" />
                  <datalist id="consultant-list"></datalist>
                </div>
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Durum</label>
                  <input id="cert-durum" name="durum" type="text" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main" placeholder="Durum" />
                </div>
                <div class="space-y-2">
                  <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Logo</label>
                  <input id="cert-logo" name="logo" type="text" class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main" placeholder="Drive dosya ID veya URL" />
                </div>
              </div>
            </div>

            <div class="rounded-3xl border border-border-main bg-surface p-5 sm:p-6 space-y-4">
              <h2 class="text-sm font-black uppercase tracking-widest text-main">Ek Bilgiler</h2>
              <div class="rounded-2xl border border-border-main bg-surface-hover p-4 text-sm leading-relaxed text-main space-y-2">
                <p><span class="text-muted">Firma:</span> <span id="cert-preview-company">—</span></p>
                <p><span class="text-muted">Yetkili:</span> <span id="cert-preview-contact">—</span></p>
                <p><span class="text-muted">E-Posta:</span> <span id="cert-preview-mail">—</span></p>
              </div>
              <label class="flex items-center gap-3 rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm font-semibold text-main">
                <input id="cert-gozetim-done" type="checkbox" class="h-4 w-4 rounded border-border-main bg-surface text-primary" />
                Gozetim yapildi mi?
              </label>
              <div class="space-y-2">
                <label class="text-[11px] font-bold uppercase tracking-widest text-muted">Notlar</label>
                <textarea id="cert-not" name="not" rows={7} class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main min-h-[180px]" placeholder="Notlar"></textarea>
              </div>
            </div>
          </section>

          <section class="grid grid-cols-1 xl:grid-cols-2 gap-6">
            <div class="rounded-3xl border border-border-main bg-surface p-5 sm:p-6 space-y-4">
              <div class="flex items-center justify-between gap-3">
                <h2 class="text-sm font-black uppercase tracking-widest text-main">Turkce Kapsam</h2>
                <div class="flex gap-2">
                  <button id="cert-kapsam-upper" type="button" class="rounded-2xl border border-border-main bg-surface-hover px-3 py-2 text-[11px] font-black uppercase tracking-widest text-main hover:bg-surface transition-colors">BUYUK</button>
                  <button id="cert-kapsam-translate" type="button" class="rounded-2xl border border-indigo-500/20 bg-indigo-500/10 px-3 py-2 text-[11px] font-black uppercase tracking-widest text-indigo-300 hover:bg-indigo-500/15 transition-colors">EN Cevir</button>
                </div>
              </div>
              <textarea id="cert-kapsam" name="kapsam" rows={9} class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main min-h-[220px]"></textarea>
            </div>

            <div class="rounded-3xl border border-border-main bg-surface p-5 sm:p-6 space-y-4">
              <div class="flex items-center justify-between gap-3">
                <h2 class="text-sm font-black uppercase tracking-widest text-main">Ingilizce Scope</h2>
                <div class="flex gap-2">
                  <button id="cert-scope-upper" type="button" class="rounded-2xl border border-border-main bg-surface-hover px-3 py-2 text-[11px] font-black uppercase tracking-widest text-main hover:bg-surface transition-colors">BIG</button>
                  <button id="cert-scope-translate" type="button" class="rounded-2xl border border-indigo-500/20 bg-indigo-500/10 px-3 py-2 text-[11px] font-black uppercase tracking-widest text-indigo-300 hover:bg-indigo-500/15 transition-colors">TR Cevir</button>
                </div>
              </div>
              <textarea id="cert-scope" name="scope" rows={9} class="w-full rounded-2xl border border-border-main bg-surface-hover px-4 py-3 text-sm text-main min-h-[220px]"></textarea>
            </div>
          </section>

          <div class="flex flex-col sm:flex-row gap-3 sm:items-center sm:justify-end">
            <button id="cert-clear" type="button" class="inline-flex items-center justify-center rounded-2xl border border-border-main bg-surface px-4 py-3 text-sm font-bold text-main hover:bg-surface-hover transition-colors">Formu Temizle</button>
            <button id="cert-submit" type="submit" class="inline-flex items-center justify-center gap-2 rounded-2xl bg-indigo-600 px-5 py-3 text-sm font-black text-white hover:bg-indigo-500 transition-colors disabled:opacity-60 disabled:cursor-not-allowed">
              <FilePlus2 size={15} />
              <span id="cert-submit-label">Sertifika Ekle</span>
            </button>
          </div>
        </form>
      </div>
    </section>
  </div>
</DashboardLayout>

<script>
  import { api } from '../../lib/api';
  import { SyncManager } from '../../lib/sync';
  import { loadCompanyOperationContext } from '../../features/company-ops/context';
  import {
    normalizeLegacyIsoRows,
    buildSuggestedCertPrefix,
    mergeCertNoSuggestion,
    addOneYearMinusOneDay,
    toDisplayDate,
    buildCertificateQrLink,
    type LegacyIsoOption,
  } from '../../features/company-ops/certificate-form';

  const params = new URLSearchParams(location.search);
  const firmaId = params.get('id') || '';
  const editId = params.get('edit') || '';
  const cloneId = params.get('clone') || '';

  const els = {
    skeleton: document.getElementById('cert-page-skeleton'),
    error: document.getElementById('cert-page-error'),
    shell: document.getElementById('cert-page-shell'),
    form: document.getElementById('certificate-form') as HTMLFormElement | null,
    backLink: document.getElementById('cert-back-link') as HTMLAnchorElement | null,
    standSelect: document.getElementById('cert-standart') as HTMLSelectElement | null,
    otherWrap: document.getElementById('other-standard-wrap'),
    otherInput: document.getElementById('cert-other') as HTMLInputElement | null,
    isoFull: document.getElementById('cert-iso-full') as HTMLInputElement | null,
    nick: document.getElementById('cert-nick') as HTMLInputElement | null,
    gst: document.getElementById('cert-gst') as HTMLInputElement | null,
    goz: document.getElementById('cert-goz') as HTMLInputElement | null,
    sno: document.getElementById('cert-sno') as HTMLInputElement | null,
    suggested: document.getElementById('cert-sno-suggested') as HTMLInputElement | null,
    kapsam: document.getElementById('cert-kapsam') as HTMLTextAreaElement | null,
    scope: document.getElementById('cert-scope') as HTMLTextAreaElement | null,
    logo: document.getElementById('cert-logo') as HTMLInputElement | null,
    dan: document.getElementById('cert-dan') as HTMLInputElement | null,
    consultantList: document.getElementById('consultant-list'),
    submit: document.getElementById('cert-submit') as HTMLButtonElement | null,
    submitLabel: document.getElementById('cert-submit-label'),
  };

  let companyContext: Awaited<ReturnType<typeof loadCompanyOperationContext>> = null;
  let standards: LegacyIsoOption[] = [];
  let editingCertificate: Record<string, any> | null = null;

  function setShellError() {
    els.skeleton?.classList.add('hidden');
    els.error?.classList.remove('hidden');
  }

  function hydrateHeader() {
    if (!companyContext) return;
    const isEditMode = Boolean(editId);
    const isCloneMode = !isEditMode && Boolean(cloneId);
    document.getElementById('cert-header-breadcrumb')!.textContent = isEditMode
      ? 'Sertifika Duzenle'
      : isCloneMode
        ? 'Sertifika Kopyala'
        : 'Sertifika Ekle';
    document.getElementById('cert-company-id')!.textContent = `M${companyContext.firmaId}`;
    document.getElementById('cert-company-nick')!.textContent = companyContext.nick || 'Firma';
    document.getElementById('cert-company-dan')!.textContent = companyContext.danisman || 'Danisman atanamamis';
    document.getElementById('cert-company-unvan')!.textContent = companyContext.unvan || companyContext.nick || `Firma #${companyContext.firmaId}`;
    document.getElementById('cert-company-address')!.textContent = [companyContext.adres, [companyContext.sehir, companyContext.ulke].filter(Boolean).join(' / ')].filter(Boolean).join(' - ') || '—';
    document.getElementById('cert-company-kapsam')!.textContent = companyContext.kapsam || 'Kapsam bulunamadi.';
    document.getElementById('cert-company-stats')!.textContent = isEditMode
      ? `${companyContext.certificates.length} sertifika arasindan #${editId} duzenleniyor.`
      : isCloneMode
        ? `${companyContext.certificates.length} sertifika arasindan #${cloneId} kopyalaniyor.`
        : `${companyContext.certificates.length} sertifika, ${companyContext.tests.length} test kaydi bulundu.`;
    document.getElementById('cert-preview-company')!.textContent = companyContext.unvan || companyContext.nick || '—';
    document.getElementById('cert-preview-contact')!.textContent = [companyContext.yetkiliAdi, companyContext.yetkiliUnvani].filter(Boolean).join(' / ') || '—';
    document.getElementById('cert-preview-mail')!.textContent = companyContext.mail || companyContext.irtibatMail || '—';
    if (els.nick) els.nick.value = companyContext.nick;
    if (els.logo) els.logo.value = companyContext.logo;
    if (els.dan) els.dan.value = companyContext.danisman;
    if (els.submitLabel) {
      els.submitLabel.textContent = isEditMode
        ? 'Sertifikayi Guncelle'
        : isCloneMode
          ? 'Kopyadan Sertifika Olustur'
          : 'Sertifika Ekle';
    }
  }

  function renderStandards() {
    if (!els.standSelect) return;
    els.standSelect.innerHTML = '<option value="">Seciniz...</option>';
    standards.forEach((item) => {
      const option = document.createElement('option');
      option.value = item.id;
      option.textContent = item.id;
      option.dataset.prefix = item.prefix;
      option.dataset.fullName = item.fullName;
      els.standSelect?.appendChild(option);
    });
    if (standards.length === 0) {
      ['9001', '14001', '45001', '22000', '27001', '13485', '10002', '50001', 'CE', 'Diğer'].forEach((value) => {
        const option = document.createElement('option');
        option.value = value;
        option.textContent = value;
        option.dataset.prefix = value;
        option.dataset.fullName = value;
        els.standSelect?.appendChild(option);
      });
    }
  }

  function updateStandardMeta() {
    if (!els.standSelect) return;
    const selected = els.standSelect.selectedOptions[0];
    const standardText = selected?.textContent?.trim() || '';
    const prefix = selected?.dataset.prefix || standardText;
    const fullName = selected?.dataset.fullName || standardText;
    if (els.isoFull) els.isoFull.value = fullName;
    if (els.suggested) els.suggested.value = buildSuggestedCertPrefix(prefix, firmaId);

    const shouldShowOther = ['DİĞER', 'DIGER', 'OTHER', 'CE'].includes(standardText.toUpperCase());
    els.otherWrap?.classList.toggle('hidden', !shouldShowOther);
    if (!shouldShowOther && els.otherInput) els.otherInput.value = '';
  }

  async function loadStandards() {
    const res = await api.getMasterData('standards');
    const rows = res?.data?.dataset?.rows || [];
    standards = normalizeLegacyIsoRows(rows);
    renderStandards();
  }

  async function loadConsultants() {
    const res = await api.call('getConsultants');
    const consultants: string[] = Array.isArray(res?.data) ? res.data : [];
    if (!els.consultantList) return;
    els.consultantList.innerHTML = '';
    consultants.forEach((name) => {
      const option = document.createElement('option');
      option.value = name;
      els.consultantList?.appendChild(option);
    });
  }

  function normalizeObjectValue(record: Record<string, any>, aliases: string[]) {
    for (const key of aliases) {
      const value = record?.[key];
      if (value !== undefined && value !== null && String(value).trim() !== '') {
        return String(value).trim();
      }
    }
    return '';
  }

  function toInputDate(value: string) {
    if (!value) return '';
    const parts = value.split('.');
    if (parts.length === 3) return `${parts[2]}-${parts[1]}-${parts[0]}`;
    return value;
  }

  function normalizeDenetimValue(value: string) {
    const trimmed = String(value || '').trim();
    if (trimmed === '1. Gozetim' || trimmed === '2. Gozetim') {
      return 'Gozetim';
    }
    return trimmed;
  }

  async function loadExistingCertificate() {
    const sourceId = editId || cloneId;
    if (!sourceId) return;
    const res = await api.getCertificateById(sourceId);
    const cert = res?.data as Record<string, any> | null;
    if (!cert) {
      alert(`Sertifika bulunamadi: ${sourceId}`);
      return;
    }

    editingCertificate = cert;

    const standardValue = normalizeObjectValue(cert, ['Standart', 'standart']);
    const otherValue = normalizeObjectValue(cert, ['Other Standard', 'Other', 'other', 'Diğer', 'diger', 'Diger', 'diğer', 'Diğer Standart', 'digerStandart']);

    if (els.standSelect) {
      const option = Array.from(els.standSelect.options).find((item) => item.textContent?.trim() === standardValue);
      if (option) els.standSelect.value = option.value;
      else if (standardValue) {
        const custom = document.createElement('option');
        custom.value = standardValue;
        custom.textContent = standardValue;
        custom.dataset.prefix = standardValue;
        custom.dataset.fullName = standardValue;
        els.standSelect.appendChild(custom);
        els.standSelect.value = standardValue;
      }
      updateStandardMeta();
    }

    if (els.otherInput) els.otherInput.value = otherValue;
    if (els.isoFull && !els.isoFull.value) els.isoFull.value = standardValue;
    (document.getElementById('cert-denetim') as HTMLSelectElement | null)!.value = normalizeDenetimValue(
      normalizeObjectValue(cert, ['Denetim Tipi', 'denetim']),
    );
    if (els.sno) els.sno.value = normalizeObjectValue(cert, ['Sertifika No', 'sNo', 'SNo', 'sno']);
    if (els.gst) els.gst.value = toInputDate(normalizeObjectValue(cert, ['Sertifika Tarihi', 'gst', 'GST']));
    if (els.goz) els.goz.value = toInputDate(normalizeObjectValue(cert, ['Gözetim Tarihi', 'Sertifika Gözetim Tarihi', 'goz', 'GOZ']));
    (document.getElementById('cert-stt') as HTMLInputElement | null)!.value = toInputDate(normalizeObjectValue(cert, ['Son Tetkik Tarihi', 'stt', 'STT']));
    (document.getElementById('cert-sgt') as HTMLInputElement | null)!.value = toInputDate(normalizeObjectValue(cert, ['Sertifika Geçerlilik Tarihi', 'sgt', 'SGT']));
    if (els.kapsam) els.kapsam.value = normalizeObjectValue(cert, ['Kapsam', 'kapsam']);
    if (els.scope) els.scope.value = normalizeObjectValue(cert, ['Scope', 'scope']);
    if (els.logo) els.logo.value = normalizeObjectValue(cert, ['Logo', 'logo']) || companyContext?.logo || '';
    (document.getElementById('cert-kod') as HTMLInputElement | null)!.value = normalizeObjectValue(cert, ['Kod', 'kod', 'NACE', 'nace']);
    (document.getElementById('cert-akreditasyon') as HTMLInputElement | null)!.value = normalizeObjectValue(cert, ['Akreditasyon', 'akreditasyon']) || 'IAKS';
    (document.getElementById('cert-akredite') as HTMLInputElement | null)!.value = normalizeObjectValue(cert, ['Akredite', 'akredite']) || 'Medicert';
    if (els.dan) els.dan.value = normalizeObjectValue(cert, ['Danışman', 'Danisman', 'dan', 'danisman']) || companyContext?.danisman || '';
    (document.getElementById('cert-durum') as HTMLInputElement | null)!.value = normalizeObjectValue(cert, ['Durum', 'durum']);
    (document.getElementById('cert-not') as HTMLTextAreaElement | null)!.value = normalizeObjectValue(cert, ['Not', 'not']);
    (document.getElementById('cert-gozetim-done') as HTMLInputElement | null)!.checked = normalizeObjectValue(cert, ['Gözetim Conf.', 'gozetimConfirmed', 'gozetimConf']).toUpperCase() === 'TRUE';

    if (cloneId) {
      const denetimSelect = document.getElementById('cert-denetim') as HTMLSelectElement | null;
      if (denetimSelect) {
        denetimSelect.value = 'Gozetim';
      }
      (document.getElementById('cert-gozetim-done') as HTMLInputElement | null)!.checked = false;
    }
  }

  function bindActions() {
    els.backLink && (els.backLink.href = firmaId ? `/company?id=${firmaId}` : '/search');

    els.standSelect?.addEventListener('change', updateStandardMeta);

    document.getElementById('cert-auto-goz')?.addEventListener('click', () => {
      if (els.goz && els.gst) els.goz.value = addOneYearMinusOneDay(els.gst.value);
    });

    document.getElementById('cert-fill-from-company')?.addEventListener('click', () => {
      if (!companyContext) return;
      if (els.kapsam) els.kapsam.value = companyContext.kapsam;
      if (els.scope) els.scope.value = companyContext.scope;
      if (els.logo) els.logo.value = companyContext.logo;
      if (els.dan) els.dan.value = companyContext.danisman;
    });

    document.getElementById('cert-apply-suggested')?.addEventListener('click', () => {
      if (!els.sno || !els.suggested) return;
      els.sno.value = mergeCertNoSuggestion(els.suggested.value, els.sno.value);
    });

    document.getElementById('cert-kapsam-upper')?.addEventListener('click', () => {
      if (els.kapsam) els.kapsam.value = els.kapsam.value.toLocaleUpperCase('tr-TR');
    });

    document.getElementById('cert-scope-upper')?.addEventListener('click', () => {
      if (els.scope) els.scope.value = els.scope.value.toLocaleUpperCase('en-US');
    });

    document.getElementById('cert-kapsam-translate')?.addEventListener('click', async () => {
      if (!els.kapsam || !els.scope || !els.kapsam.value.trim()) return;
      const res = await api.translate(els.kapsam.value, true);
      if (res.success && els.scope) els.scope.value = String(res.data || '');
      else alert(`Ceviri hatasi: ${res.error}`);
    });

    document.getElementById('cert-scope-translate')?.addEventListener('click', async () => {
      if (!els.scope || !els.kapsam || !els.scope.value.trim()) return;
      const res = await api.translate(els.scope.value, false);
      if (res.success && els.kapsam) els.kapsam.value = String(res.data || '');
      else alert(`Ceviri hatasi: ${res.error}`);
    });

    document.getElementById('cert-clear')?.addEventListener('click', () => {
      els.form?.reset();
      if (companyContext) {
        hydrateHeader();
      }
      renderStandards();
      updateStandardMeta();
      if (editId || cloneId) {
        void loadExistingCertificate();
      } else {
        if (els.kapsam) els.kapsam.value = '';
        if (els.scope) els.scope.value = '';
        if (els.logo) els.logo.value = companyContext?.logo || '';
        if (els.dan) els.dan.value = companyContext?.danisman || '';
      }
    });

    els.form?.addEventListener('submit', async (event) => {
      event.preventDefault();
      if (!companyContext || !els.standSelect || !els.submit) return;

      const selected = els.standSelect.selectedOptions[0];
      const standardText = selected?.textContent?.trim() || '';
      if (!standardText) {
        alert('Lutfen bir standart secin.');
        return;
      }

      const certInfo = {
        nick: companyContext.nick,
        firmano: companyContext.firmaId,
        standart: standardText,
        other: els.otherInput?.value.trim() || '',
        denetim: normalizeDenetimValue((document.getElementById('cert-denetim') as HTMLSelectElement | null)?.value || ''),
        sno: els.sno?.value.trim() || '',
        gst: toDisplayDate(els.gst?.value || ''),
        goz: toDisplayDate(els.goz?.value || ''),
        stt: toDisplayDate((document.getElementById('cert-stt') as HTMLInputElement | null)?.value || ''),
        sgt: toDisplayDate((document.getElementById('cert-sgt') as HTMLInputElement | null)?.value || ''),
        kapsam: els.kapsam?.value.trim() || '',
        scope: els.scope?.value.trim() || '',
        logo: els.logo?.value.trim() || '',
        kod: (document.getElementById('cert-kod') as HTMLInputElement | null)?.value.trim() || '',
        akreditasyon: (document.getElementById('cert-akreditasyon') as HTMLInputElement | null)?.value.trim() || '',
        akredite: (document.getElementById('cert-akredite') as HTMLInputElement | null)?.value.trim() || '',
        dan: els.dan?.value.trim() || '',
        durum: (document.getElementById('cert-durum') as HTMLInputElement | null)?.value.trim() || '',
        not: (document.getElementById('cert-not') as HTMLTextAreaElement | null)?.value.trim() || '',
        gozetimConfirmed: (document.getElementById('cert-gozetim-done') as HTMLInputElement | null)?.checked ? 'TRUE' : 'FALSE',
        qr: buildCertificateQrLink(companyContext.unvan || companyContext.nick, (els.isoFull?.value || standardText), els.sno?.value || ''),
      };

      els.submit.disabled = true;
      const originalText = els.submit.innerHTML;
      els.submit.innerHTML = 'Kaydediliyor...';

      const response = editId
        ? await api.updateCertificate(editId, certInfo)
        : await api.addCertificate(certInfo);
      if (response.success) {
        await SyncManager.checkAndSync();
        alert(
          editId
            ? 'Sertifika basariyla guncellendi.'
            : cloneId
              ? 'Sertifika kopyadan yeni kayit olarak eklendi.'
              : 'Sertifika basariyla eklendi.',
        );
        window.location.href = `/company?id=${encodeURIComponent(companyContext.firmaId)}`;
        return;
      }

      alert(`Hata: ${response.error}`);
      els.submit.disabled = false;
      els.submit.innerHTML = originalText;
    });
  }

  async function boot() {
    if (!firmaId) {
      setShellError();
      return;
    }

    companyContext = await loadCompanyOperationContext(firmaId);
    if (!companyContext) {
      setShellError();
      return;
    }

    hydrateHeader();
    await Promise.all([loadStandards(), loadConsultants()]);
    await loadExistingCertificate();
    bindActions();

    els.skeleton?.classList.add('hidden');
    els.shell?.classList.remove('hidden');
  }

  void boot();
</script>
