name = "portal-api"
main = "src/workers/proxy.js"
compatibility_date = "2025-01-01"

[triggers]
crons = ["0 4 * * SUN"]

# Tenant-specific routes, bindings and non-secret vars live in
# wrangler.<tenant>.toml files. Example:
#   npx wrangler deploy --config wrangler.medicert.toml
